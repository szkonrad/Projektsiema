{
  "name": "Projektsiema",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NUXT_HOST=localhost NUXT_PORT=3000 nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve"
  },
  "dependencies": {
    "nuxt": "^2.13.3"
  },
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
